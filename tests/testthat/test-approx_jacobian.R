context("test-approx_jacobian_epsilon")

n <- 100
target <- rnorm(n, 4, 2)

# simulated_function <- function(theta){
#   rnorm(n, theta[1], theta[2])
# }
#
# approx_jacobian_epsilon(
#   theta = c("mu" = 3, "sigma" = 1),
#   transform_simulation = simulated_function,
#   target = target,
#   step = 1e-6)


